[phases.install]
cmds = ["npm install -g pnpm@9.15.9", "pnpm install --frozen-lockfile"]

[phases.build]
cmds = ["pnpm --filter @skincare/backend build"]

[start]
cmd = "pnpm --filter @skincare/backend start"